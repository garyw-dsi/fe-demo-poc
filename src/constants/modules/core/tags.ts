/**
 * @constant NEXT_TAG_TAGS
 * @description
 * it use for cache management using tag
 * because it integrate to all services
 * so it easier to manage cache by using next tag
 * @type {string[]}
 */
export const  NEXT_TAG_TAGS: string[] = ["tag-options"];